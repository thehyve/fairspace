# External dependencies for running the fairspace
external:
  keycloak:
    baseUrl: https://keycloak.demo.fairway.app
    username: keycloak
    password: keycloak-demo-123
    realm: demo
    clientId: fairspace-demo-private
    clientSecret: db67da94-fa31-47cd-8c42-0ed295790841
  elasticsearch:
    enabled: true
    transport:
      host: fairspace-demo-elasticsearch-discovery.fairspace-demo.svc.cluster.local
    rest:
      host: fairspace-demo-elasticsearch-client.fairspace-demo.svc.cluster.local

# Settings for the fairspace
fairspace:
  name: "Demo Fairspace"
  description: "Demo Fairspace"
  ingress:
    domain: fairspace.demo.fairway.app
  enableExperimentalFeatures: true
  configurationScripts:
    keycloak:
      pullPolicy: IfNotPresent

services:
  jupyterhub: https://jupyter.fairspace.demo.fairway.app


# Specific settings for Saturn subchart
saturn:
  nodes:
    - name: node1
      persistence:
        files:
          size: 60Gi
          storageClass: expandable
        database:
          size: 60Gi
          storageClass: expandable
      resources:
        limits:
          cpu: 1
          memory: 16Gi
        requests:
          cpu: 500m
          memory: 512Mi
    - name: node2
      persistence:
        files:
          size: 60Gi
          storageClass: expandable
        database:
          size: 60Gi
          storageClass: expandable
      resources:
        limits:
          cpu: 1
          memory: 16Gi
        requests:
          cpu: 500m
          memory: 512Mi
  image:
    pullPolicy: Always
  customStorageClass:
    create: true
    name: expandable
    type: pd-standard
    provisioner: kubernetes.io/gce-pd
    allowVolumeExpansion: true
  mail:
    mail.from: janneke@thehyve.nl
    mail.user: AKIAISXHGXUIMMU75YVA
    mail.password: Avjpey3XBSvnlPfWGTS2qEjiJykvbDJtj6zWW3fCZILl
    mail.transport.protocol: smtp
    mail.smtp.auth: true
    mail.smtp.host: email-smtp.us-east-1.amazonaws.com
    mail.smtp.port: 587
    mail.smtp.starttls.enable: true


# Specific settings for Mercury subchart
mercury:
  image:
    pullPolicy: Always
# Specific settings for Mercury subchart
docs:
  image:
    pullPolicy: Always
