# External dependencies for running the fairspace
external:
  keycloak:
    baseUrl: https://keycloak.ci.fairway.app
    username: keycloak
    password: keycloak-ci-123
    realm: ci
    clientId: fairspace-ci-private
    clientSecret: 4c4e5e3b-a6ed-45fd-a394-46c4f5eea510
    serviceAccount:
      password: fairspace123
  elasticsearch:
    enabled: true
    transport:
      host: fairspace-ci-elasticsearch-discovery.fairspace-ci.svc.cluster.local
    rest:
      host: fairspace-ci-elasticsearch-client.fairspace-ci.svc.cluster.local

# Settings for the fairspace
fairspace:
  name: "CI Fairspace"
  description: "CI Fairspace"
  ingress:
    domain: fairspace.ci.fairway.app
  enableExperimentalFeatures: false
  configurationScripts:
    keycloak:
      configurationScriptsTag: 0.1.9-SNAPSHOT
      pullPolicy: Always

services:
  jupyterhub: https://jupyter.fairspace.ci.fairway.app

# Specific settings for Saturn subchart
saturn:
  persistence:
    files:
      size: 60Gi
      storageClass: expandable
    database:
      size: 60Gi
      storageClass: expandable
  resources:
    limits:
      cpu: 1
      memory: 16Gi
    requests:
      cpu: 500m
      memory: 512Mi
  image:
    pullPolicy: Always
  customStorageClass:
      create: true
      name: expandable
      type: pd-standard
      provisioner: kubernetes.io/gce-pd
      allowVolumeExpansion: true
  mail:
    mail.from: pavel@thehyve.nl
    mail.user: AKIAISXHGXUIMMU75YVA
    mail.password: Avjpey3XBSvnlPfWGTS2qEjiJykvbDJtj6zWW3fCZILl
    mail.transport.protocol: smtp
    mail.smtp.auth: true
    mail.smtp.host: email-smtp.us-east-1.amazonaws.com
    mail.smtp.port: 587
    mail.smtp.starttls.enable: true


# Specific settings for Saturn subchart
saturn:
  image:
    pullPolicy: Always
# Specific settings for Docs subchart
docs:
  image:
    pullPolicy: Always
