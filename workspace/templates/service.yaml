apiVersion: v1
kind: Service
metadata:
  name: {{ template "vre.fullname" . }}
  labels:
    app: {{ template "vre.name" . }}
    chart: {{ template "vre.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "vre.name" . }}
    release: {{ .Release.Name }}
