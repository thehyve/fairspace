
# 0 - type (optional)

PREFIX fs: <http://fairspace.io/ontology#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

CONSTRUCT {?s a ?0; rdfs:label ?l}
WHERE {
    { ?s a ?0 }
    GRAPH ws:vocabulary {?0 fs:fairspaceEntity true .}
    OPTIONAL { ?s rdfs:label ?l}
}