# param0 - path
PREFIX fs: <http://fairspace.io/ontology#>
SELECT ?iri ?path ?type ?size ?created ?modified ?creator
WHERE { ?iri fs:filePath ?param0; a ?type; fs:fileSize ?size; fs:dateCreated ?created; fs:dateModified ?modified; fs:creator ?creator.
BIND(?param0 as ?path) }