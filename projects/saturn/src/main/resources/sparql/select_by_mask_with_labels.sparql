# Select by mask
# 0 - graph
# 1 - subject
# 2 - predicate
# 3 - object

PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

CONSTRUCT {
?1 ?2 ?3 .
?3 rdfs:label ?objectLabel . }
WHERE {
  GRAPH ?0 { ?1 ?2 ?3 }
  OPTIONAL { GRAPH ?0  {?3 rdfs:label ?objectLabel} }
}