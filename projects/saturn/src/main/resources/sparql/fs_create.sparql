# param0 - IRI
# param1 - path
# param2 - size
# param3 - blobId

PREFIX fs: <http://fairspace.io/ontology#>

INSERT { ?param0 a fs:File; fs:filePath ?param1; fs:fileSize ?param2; fs:blobId ?param3; fs:dateCreated ?date; fs:dateModified ?date . }
WHERE { BIND(NOW() as ?date) }