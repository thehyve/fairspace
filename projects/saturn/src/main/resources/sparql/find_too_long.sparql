# Find a subject violating sh:maxLength constaraint
# 0 - property
# 1 - a list of allowed subject's classes
# 2 - maxLength

SELECT *
WHERE {
 ?subject a ?subjectClas;
 %1$s ?object .

 FILTER ( strlen(?object) >  %3$s)
 VALUES ?subjectClass {%2$s}
}
LIMIT 1