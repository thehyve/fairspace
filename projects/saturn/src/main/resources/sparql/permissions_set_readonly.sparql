# Sets a permission
# 0 - the resource
# 1 - the value (true or false)

PREFIX fs: <http://fairspace.io/ontology#>

DELETE { GRAPH ws:permissions { %1$s fs:readOnly ?value}} WHERE { GRAPH ws:permissions { %1$s fs:readOnly ?value}} ;
INSERT DATA { GRAPH ws:permissions {%1$s fs:readOnly %2$s } }