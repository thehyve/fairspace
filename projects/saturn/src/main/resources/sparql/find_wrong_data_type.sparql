# Find a subject violating sh:datatype constaraint
# 0 - property
# 1 - a list of allowed subject's classes
# 2 - datatype

SELECT *
WHERE {
 ?subject a ?subjectClass ;
 %1$s ?object .

 FILTER ( datatype(?object) !=  %3$s)
 VALUES ?subjectClass {%2$s}
}
LIMIT 1
