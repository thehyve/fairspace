export const pending = type => type + '_PENDING';
export const fulfilled = type => type + '_FULFILLED';
export const rejected = type => type + '_REJECTED';
export const invalidate = type => 'INVALIDATE_' + type;
