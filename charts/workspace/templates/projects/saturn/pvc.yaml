{{- if and .Values.saturn.persistence.enabled (not .Values.saturn.persistence.existingClaim) -}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "saturn.fullname" . }}
  labels:
    app: saturn
    chart: {{ template "workspace.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.saturn.persistence.annotations }}
  annotations:
{{ toYaml .Values.saturn.persistence.annotations | indent 4 }}
{{- end }}
spec:
  accessModes:
    - {{ .Values.saturn.persistence.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.saturn.persistence.size | quote }}
{{- if .Values.saturn.persistence.storageClass }}
{{- if (eq "-" .Values.saturn.persistence.storageClass) }}
  storageClassName: ""
{{- else }}
  storageClassName: "{{ .Values.saturn.persistence.storageClass }}"
{{- end }}
{{- end }}
{{- end -}}
