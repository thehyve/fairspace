{{- $ingress := .Values.workspace.ingress -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "workspace.name" . }}-docs-ingress
{{- with $ingress.annotations.docs }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app: {{ template "workspace.name" . }}
    chart: {{ template "workspace.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
spec:
{{- if $ingress.tls.enabled }}
  tls:
    - hosts:
      - {{ template "docs.hostname" $ }}
      secretName: {{ template "workspace.tlsSecretName" $ }}
{{- end }}
  rules:
    - host: {{ template "docs.hostname" $ }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ template "docs.fullname" . }}
              servicePort: 80
