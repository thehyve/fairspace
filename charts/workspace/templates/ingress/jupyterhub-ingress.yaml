{{- $ingress := .Values.workspace.ingress -}}
{{- if $ingress.enabled -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "workspace.name" . }}-jupyterhub-ingress
{{- with $ingress.annotations.jupyter }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app: {{ template "workspace.name" . }}
    chart: {{ template "workspace.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
spec:
{{- if $ingress.tls.enabled }}
  tls:
    - hosts:
      - {{ template "jupyterhub.hostname" $ }}
      secretName: {{ template "workspace.tlsSecretName" $ }}
{{- end }}
  rules:
    - host: {{ template "jupyterhub.hostname" $ }}
      http:
        paths:
          - path: /
            backend:
              serviceName: proxy-public
              servicePort: 80
{{- end -}}




